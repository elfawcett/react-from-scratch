import React from 'react';
import ReactDOM from 'react-dom';

import { App } from './components/App';

/* const store = some redux stuff */

ReactDOM.render(<App /*store={store}*/ />, document.getElementById('root'));
